<div class='row'>
  <div class='large-12 small-12 columns'>
    <h1>Users</h1>
    <hr />
	<% @user_names.each do |name| %>
		<p><%= link_to name.first_name, name %> <%= link_to name.last_name, name %></p>
	<% end %>
  </div>
</div>